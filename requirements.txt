# Tested with Python 3.10 / 3.11 (SDV does not yet support 3.14).
pandas
numpy
scikit-learn
scipy         # already pulled in transitively via scikit-learn; imported directly for the
              # SCD duration-fidelity KS-test (synth_eval.scd.scd_duration_fidelity)
matplotlib
seaborn
sdv          # pulls in ctgan + torch, needed for CTGAN/TVAE/CopulaGAN (torch also used directly by TabSyn, synth_eval/tabsyn.py)
tqdm         # already pulled in transitively via sdv/ctgan; TabSyn imports it directly for its own training progress bars
sdmetrics
fastapi    # dashboard backend: `uvicorn server:app` (UI in web/index.html)
uvicorn
python-multipart   # CSV upload endpoint
openai            # chat assistant: talks to DeepSeek's OpenAI-compatible API
python-dotenv     # chat assistant: loads DEEPSEEK_API_KEY from .env
# Optional: enables exporting SDV/plotly figures to PNG.
# kaleido
