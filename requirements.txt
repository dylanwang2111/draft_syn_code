# Tested with Python 3.10 / 3.11 (SDV does not yet support 3.14).
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
sdv>=1.9          # pulls in ctgan + torch, needed for CTGAN/TVAE/CopulaGAN
sdmetrics>=0.14
fastapi>=0.110    # dashboard backend: `uvicorn server:app` (UI in web/index.html)
uvicorn>=0.29
python-multipart>=0.0.9   # CSV upload endpoint
# Optional: enables exporting SDV/plotly figures to PNG.
# kaleido>=0.2
